# sources/

Raw source layer — one markdown file per source, immutable once committed.
Use the header format defined in [CLAUDE.md](../CLAUDE.md): title, URL,
access date, region, type, key facts with exact quoted figures, and a
two-sentence summary. Don't copy whole articles; capture the numbers and
where they came from.
